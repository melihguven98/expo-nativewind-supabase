---
name: Supabase Guide
description: This skill should be used when the user asks to "query database", "supabase query", "fetch data", "insert data", or mentions tables, RLS, or Supabase client.
version: 0.1.0
---

# Supabase Usage

- Client: `import { supabase } from '@/lib/supabase'`
- Use `.single()` for single records
- Always handle errors
- Follow RLS policies
